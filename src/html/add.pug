extends components/_layout.pug

block title
	title Добавление

block metaInfo
	-pageMeta = {"keywords": "", "description": ""}
	+pageMetaMxn

block content
	.container
		.row
			.col-md-4.col-sm-6.col-xs-12
				form
					.form-group
						label(for="number") Номер Автомобиля
						input(type="text" name="number" placeholder="Введите номер автомобиля").form-control
					.form-group
						label(for="interval") Интервал
						input(type="text" name="interval" placeholder="0").form-control
					.form-group
						label(for="name") Имя
						input(type="text" name="name" placeholder="Введите ваше имя").form-control
					.form-group
						label(for="phone") Телeфон
						input(type="text" name="phone" placeholder="0").form-control
					.form-group
						label(for="email") Электронная почта
						input(type="email" name="email" placeholder="Введите эл. почту").form-control
					.form-group
						label(for="") Group
						select(name="" placeholder="0").form-control
							option 1
							option 2
							option 3
					.form-group
						.checkbox
							label
								input(type="checkbox")
								| Проверить после добавления
					button(type="submit").btn.btn-transp.col-md-12.col-sm-12.col-xs-12 Изменить